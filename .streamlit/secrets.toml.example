[auth]
username = "your_username"
hashed_password = "bcrypt_hash_here"  # generate with: python -c "import bcrypt; print(bcrypt.hashpw(b'yourpassword', bcrypt.gensalt(12)).decode())"

[demo]
username = "test"
hashed_password = "bcrypt_hash_here"  # bcrypt hash of demo password
